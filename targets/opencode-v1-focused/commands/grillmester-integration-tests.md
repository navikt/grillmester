---
description: "Designs narrow integration tests that exercise a real adapter or boundary through its public interface without booting the whole application. Use for repository, database, HTTP-client, message-adapter, filesystem, or similar contract tests; use the repository's full-system test workflow when the assembled app must boot."
---

Use the `skill` tool to load `grillmester-integration-tests`, then follow that skill for this request.

Treat the following as the user's arguments to the skill:

$ARGUMENTS
