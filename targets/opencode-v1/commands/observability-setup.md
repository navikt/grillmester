---
description: "Add or improve service metrics, structured logs, traces, health signals, dashboards and alerts. Use when instrumentation or operational visibility is missing or misleading; use `nav-troubleshoot` to investigate an active incident with existing telemetry."
---

Use the `skill` tool to load `observability-setup`, then follow that skill for this request.

Treat the following as the user's arguments to the skill:

$ARGUMENTS
