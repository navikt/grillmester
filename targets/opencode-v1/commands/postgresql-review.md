---
description: "Design or review PostgreSQL schemas, queries, transactions, migrations and connection capacity. Use for slow queries, pool sizing, data integrity or database changes; use `nav-troubleshoot` for deployed incident evidence and `architecture-review` for broader storage decisions."
---

Use the `skill` tool to load `postgresql-review`, then follow that skill for this request.

Treat the following as the user's arguments to the skill:

$ARGUMENTS
