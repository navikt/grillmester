---
description: "Create narrow tests through a real adapter or boundary using its public interface. Use for database, HTTP-client, message-adapter or filesystem contracts; use `e2e-tests` when proving the flow requires booting the assembled application."
---

Use the `skill` tool to load `integration-tests`, then follow that skill for this request.

Treat the following as the user's arguments to the skill:

$ARGUMENTS
