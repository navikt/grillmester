---
description: "Create a compact temporary handoff so a fresh OpenCode session can continue the current work."
---

Use the `skill` tool to load `grillmester-handoff`, then follow that skill for this request.

Treat the following as the user's arguments to the skill:

$ARGUMENTS
