---
description: "Create full-system tests that boot the application and prove an observable user or system flow. Use when the assembled app, its entry point and final outcome must work together; use `integration-tests` for a narrow real adapter without full application boot."
---

Use the `skill` tool to load `e2e-tests`, then follow that skill for this request.

Treat the following as the user's arguments to the skill:

$ARGUMENTS
